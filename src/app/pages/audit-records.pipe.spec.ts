import { AuditRecordsPipe } from './audit-records.pipe';

describe('AuditRecordsPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditRecordsPipe();
    expect(pipe).toBeTruthy();
  });
});
